#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main(){
  char palavra[50] = {"Bom dia, Simpatia"};

printf("\nAntes: %s\n", palavra);

strupr(palavra);

printf("\nDepois: %s\n", palavra);







}